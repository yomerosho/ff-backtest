# Copy this file to `.env` and fill in your key. `.env` is gitignored.
#   cp .env.example .env
# Only the live LLM paths need this (run_backtest.py --live-llm,
# run_backtest_enriched.py, predict_roster.py). The --demo path needs nothing.

ANTHROPIC_API_KEY=
