# Complete manifest - Streamlit Cloud installs from this file, so everything the
# dashboard needs must be listed here.
pandas>=2.0
numpy>=1.24
pyarrow>=14
streamlit>=1.40
anthropic>=0.40
python-dotenv>=1.0
matplotlib>=3.7      # only for run_backtest.py's calibration plot
